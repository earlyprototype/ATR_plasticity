# Claim register

*Every claim this project makes, what supports it, and its current status. This file is the
source of truth for what the repository asserts. Prose documents describe measurements;
**this file decides what those measurements are allowed to be called.***

Introduced by `ALIGNMENT_REVIEW.md`, which explains why: the same measurement was corrected
in one document and left stale in three others, and a completed measurement (C-11) was
recorded as unrun in three places. Nothing here is new evidence — every row cites an artifact
already in the repo.

## How to use this file

- **A claim enters the register before it enters any prose document.** If it is not here, it
  is not something the project asserts.
- **Write-ups cite row IDs**, not restatements. `EXP_001_RESULTS.md` describes what was
  measured; the register says what it means.
- **Changing a row's status is a pull request of its own**, with the evidence, and it goes
  through peer review. A status change is a bigger event than a new measurement.
- **`retired` rows are never deleted.** The record of what the project used to believe is
  part of the evidence.
- **Every row carries its caveat.** If the caveat cannot travel with the claim, the claim is
  not ready to leave this file.

**Two explicit exceptions to "every row cites a committed artifact":**

- **`open` rows cite no evidence, by definition.** They are questions the project intends to
  answer, not things it asserts. Their Evidence column reads `—` and their caveat names the
  experiment that would populate it. C-54 is the only one still awaiting its experiment; C-50 was
  answered by T1.4 (answer C-55), C-51 by T1.1 (answer C-56), C-52 by T1.2 (no hysteresis; corroborates C-26/C-56), C-35 by T2.1 (answer C-58), and C-53 by EXP-002 (answers C-61, C-62). An `open` row may never be quoted as a claim.
- **A row may cite a gap rather than an artifact** when the claim *is* about an absence — C-43
  ("not yet in `PRIOR_ART.md`") is the example. The evidence for an absence is the search that
  failed to find it, which is why T0.6 exists: commit the search artifacts and these rows get
  real evidence.

**Bootstrap note.** `ALIGNMENT_REVIEW.md` was written before this register existed, so its
findings F1–F11 are numbered independently and do not cite C-rows. The mapping is one-way and
recorded here: F1 → C-30…C-34, F2 → C-26…C-28, F3 → C-04…C-07, F4 → C-10, C-22, C-23,
F5 → C-13…C-15, F6 → C-41, F7 → C-44, F8 → C-47, F10 → C-42, C-43, F11 → C-45, C-46, C-57. Every
document written *after* this file cites row IDs directly.

**Standing rule 3 is bounded by C-63.** The severed-path control's floor is exactly zero at
**one whole-matrix** plastic site (at a head site it is float32 noise, not zero — C-45, C-57)
and is **not** zero at more than one: a lower plastic layer's drift reaches a
higher one within a single forward pass, which severing the loop does not cut. Any multi-site
closed-versus-offline number therefore has no zero baseline, and must not be placed in a series
with the single-site shares (C-31, C-58).

## Status vocabulary

| Status | Means |
|---|---|
| `supported` | Measured, controlled, reproducible from a committed artifact. Quotable with its caveat. |
| `provisional` | Measured, but a named control or replication is missing. Quotable only with the gap stated. |
| `not-established` | Asserted somewhere in the repo without sufficient evidence. **Not quotable.** |
| `retired` | Was asserted, now contradicted or superseded. Kept for the record. |
| `open` | A question the project intends to answer. No claim yet. |

---

## A — The landscape (frozen, no plasticity)

| ID | Claim | Status | Evidence | Caveat that must travel with it |
|---|---|---|---|---|
| **C-01** | 125 prompts settle into 5 basins: `prolet` 55, `Divine` 34, `till` 19, `Anarch` 16, `solidarity` 1 | `supported` | `output_baseline/basins.jsonl`, `BASELINE.md` | Basin = top-1 token of the settled state. 69/125 sit at a top1−top2 margin below 0.5 |
| **C-02** | `Divine` is an exact period-2 limit cycle; all other basins are fixed points | `supported` | `BASELINE.md`; `instrument_validation.json` (lag-1 0.684912, lag-2 1.000000) | "Exact" means indistinguishable from period-2 at the float32 floor (`1−cos` 1.24e-14 to 5.21e-14 over 39 probes, median 2.08e-14 — the 1.5e-14 once quoted here is the low end, not the centre), not bit-identical |
| **C-03** | The `Divine` orbit is attracting and wide — 5/5 perturbation magnitudes return | `supported` | `RESONANCE_NOTE.md`; return criterion fixed before running | Contraction is **not** a constant. Per-decade rate ranges 4–124 across the ladder; the quoted 0.968 / 71-per-decade is an endpoint slope, not a fit. `EXP_001_RESULTS.md` §6 measures 0.9521 (47/decade) on a different trajectory |
| **C-04** | Within a basin, prompts settle onto **nearby-but-distinct** states — compression, not erasure | `supported` | `BASELINE.md` "Within-basin spread": mean `1−cos` 3.32e-03 over 2337 pairs | **Answers issue #31.** Interpretation was fixed in advance, as the issue required |
| **C-05** | Within-basin variation is essentially **one-dimensional** | `supported` | `BASELINE.md`: participation ratio 1.02–1.29 against a maximum of n−1 = 15–54 | |
| **C-06** | Position uniformity holds for **every** basin, not only `Divine` | `supported` | `BASELINE.md`: 125/125 prompts fully uniform (all position pairs above cos 0.999) | Corrects the parent's framing, which reports it for `Divine` |
| **C-07** | **The basin taxonomy has a resolution limit, and it is comparable to the basin separation itself** | `supported` | `BASELINE.md`: **pooled** mean within-basin spread 3.319e-03, over 2337 pairs across all five basins, vs the nearest-basin gap (`Anarch`–`prolet`) 2.874e-03 — **ratio 1.15** | Still the caveat to carry, but **read the decomposition before applying it to a specific pair, because the pooled figure is driven by one basin**. Per-basin within spreads (`BASELINE.md` "Within basin"): `Divine` **6.128e-03**, `prolet` **2.773e-03**, `Anarch` **1.178e-03**, `till` **3.493e-04**. Only `Divine` exceeds the 2.874e-03 nearest-basin gap; the other three are below it, and `Divine` is the period-2 basin, whose spread is intrinsically the largest. So the headline form — two basins no further apart than the prompts inside one of them — is true with "one of them" being `Divine`, which is **not** one of the two nearest basins. For the nearest pair, `Anarch` and `prolet`, both spreads sit below their mutual gap and the pair resolves. **Added 2026-08-05**: this decomposition was not previously stated and the pooled ratio was being applied pairwise, which it does not license. Whether C-07 should be restated or split is an operator decision |

---

## B — What the rules do

**Convention for C-10 – C-12.** The code stores `W` as `(n_in, n_out)` and computes
`y = x @ W + b` with `x` a **row** vector, so `E[x xᵀ]` and `E[x yᵀ]` below denote the
`(n_in, n_in)` and `(n_in, n_out)` outer-product averages `E[xᵀx]/n` and `E[xᵀy]/n` in code
terms — matching `plasticity.py:131-134` (`_hebb_term`), with the rule table at `:64-66`. Equivalent column-vector form: `y = Wᵀx + b`.

| ID | Claim | Status | Evidence | Caveat |
|---|---|---|---|---|
| **C-10** | The Hebbian update is exactly `ΔW = E[x xᵀ]·W + E[x]·b_outᵀ = C·W + x̄ b_outᵀ` — power iteration on the site's input second-moment matrix **plus a rank-1 bias term** | `supported` | `plasticity.py:474` and `:507` (y is `hook_mlp_out` = `x@W + b_out`), `:131-134` (`_hebb_term`, `ΔW = E[x yᵀ]`). Bias share `‖b_out‖/‖ȳ‖` = 3.353/19.451 = **17%**. Discriminating measurement: `u_right_sign_ref_cos` gives cos(v₁, ȳ) = **0.999999** in the severed cells, where a bias-free `v₁ ∝ Wᵀx̄` would give 0.9975 | **The bias-free form `ΔW = C·W` is retired** — it was refuted by the repo's own artifact by 3–4 orders of magnitude. Consequence: ΔW is a rank-1 edit whose output-side factor is **ȳ**, not `Wᵀx̄`, so ~17% of the direction EXP-001 decodes through the unembedding is a prompt-independent layer constant. Rank-1-ness (95.8% routed, 100.0% severed, 81–84% in the episode cells) follows from position-uniformity alone and does **not** test the `C·W` form |
| **C-11** | "No loss" means **no externally specified objective**, not no objective | `supported` | `∂/∂W ½E‖xW+b‖² = E[x yᵀ]` exactly — plain Hebb is gradient ascent on output energy, and the bias does not disturb the identity | **Oja clause narrowed:** the "same under a norm constraint" equivalence is exact for Oja's *single-unit* rule. The 768-output subspace rule this repo runs is a constrained gradient flow only when `WᵀW = I`, which fails here (`WᵀW` diagonal mean 35.4, stable rank 31.0) |
| **C-12** | `anti_hebb` negates the reinforcement term only and keeps the brake; the brake genuinely contracts | `supported` | `plasticity.py:1173` computes `−H − D`; brake read against the live effective weight; `tests/test_antihebbian.py` holds both failure directions | Bounded fixed point at `W* = −E[x yᵀ]E[y yᵀ]⁻¹` |
| **C-13** | `oja` is **inert** at this site — it moves the basin at no step size tested up to **2.9% drift with the ceiling silent** (0.0% clip) | `supported` | `step_size_map.jsonl`, the 5 `oja` cells at clip rate 0.0% | One site, one prompt. The likely mechanism (C-14) is untested. **Quote only the clean cells**: `oja` also stays `prolet` at 5% drift and 100% clip, but per the standing prohibition below that is a measurement of the ceiling and is a diagnostic note, not evidence |
| **C-14** | Oja is inert **because** the decay term dominates the reinforcement term ~110:1 at this site | `not-established` | Ratio measured at `blocks.6.mlp` only; never tested as an explanation | Plausible and important. See T2.4 |
| **C-15** | Raw Hebb "diverges immediately, unbounded weight growth" | `retired` | Contradicted: at the working point, 120 updates give 0.0% clip, 0 non-finite, ‖W‖_F +0.03% | **Propagation closed 2026-08-01.** Was still asserted in `README.md` "Why Oja rather than Hebb", `plasticity.py`'s rule header and `mode` docstring, `controls.py:c3_divergence_demo`, `ORIENTATION.md`, and `HANDOVER.md`'s file table; all now carry the corrected form. C3's premise is rescoped rather than removed: with the ceiling lifted (`max_delta_frac=1e9`) at eta ~1e-3, roughly 14× the working point, Hebb's drift grows monotonically and super-linearly while Oja's saturates. **That is the boundary of the evidence**: C3 runs a fixed, small number of applications, so it records continued growth over the run measured and does **not** establish an unbounded limit. No document may say Hebb grows "without bound"; the licensed form is that its drift keeps growing over the applications C3 measures, in that regime. `DESIGN.md` is included in the propagation set for the same reason. Note also that at every stable eta Oja's update is ~100× **larger** than Hebb's in absolute terms, so the pointwise form of the claim is false and is not asserted anywhere |

---

## C — The basin-flip result

| ID | Claim | Status | Evidence | Caveat |
|---|---|---|---|---|
| **C-20** | A `hebb` edit of ~1.1% relative Frobenius norm at `blocks.6.mlp`, with the ceiling silent, changes the settled basin | `supported` | `output_exp001/exp001.jsonl`; `step_size_map.jsonl` | 3 independent prompts. `clipped` False on every arm of every cell |
| **C-21** | There are **two** ceiling-silent cells that flip the basin, not one | `supported` | `step_size_map.jsonl`: `hebb`@7.07e-05 (1.12% drift) and `hebb`@1.18e-04 (2.20% drift), both 0.0% clip, both `comrade` | **Corrects** the "only cell in the whole sweep" claim in `EXP_001_RESULTS.md` and `HANDOVER.md` §3.2. **Not an independent replication** — both cells share prompt, seed, site and cadence, so this is robustness of the flip across a 1.7× change in eta. **Propagation closed 2026-08-05.** Both documents had kept the wrong count in the body under a top-of-file notice; both now state the count of two in place. The trailing clause this caveat used to carry, "currently suppressed by an error", was lifted from `ALIGNMENT_REVIEW.md` E1, where it described the *wording* suppressing a robustness result — not this row's status, which has been `supported` throughout |
| **C-22** | The basin flip requires **both** a sufficient magnitude **and** the right sign on the `hebb`/`oja` axis — neither alone | `retired` | **Refuted by T1.4, the test this row itself named as deciding** (`experiments/output_rank1_random/T1_4_RESULTS.md`, `rank1_random.jsonl`, `meta.json`): **4 of the 6** rank-1 random directions matched to `hebb`'s loop displacement within ±2% flip the basin — arbitrary directions, no particular sign on the `hebb`/`oja` axis. The 4 unmatchable seeds (2 at the search's scale cap, 2 straddling a displacement discontinuity) all flipped below target. The magnitude clause survives — Arm A at matched σ₁ gives 0/10 flips at displacements 3–5 orders below `hebb`'s — the sign clause does not | Second retirement on this row: "direction, not magnitude" fell to F2's α-sweep; "magnitude **and** sign" now falls to T1.4. The observations previously cited here (`anti_hebb`@2.944e-05 at 94% displacement not flipping; `hebb` at quarter displacement not flipping) remain true as measurements — what fell is the inference that the sign is *required*. What survives is narrower and lives in **C-55**: no random direction reaches `comrade`, and matching the displacement costs 66×–171× `hebb`'s relative weight change. n = 1 prompt, 1 site |
| **C-23** | The norm-matched isotropic `random` arm establishes direction-specificity | `retired` | `random` holds σ₁/‖ΔW‖_F = **0.054** at every eta against `hebb`'s 0.979 — its operator norm never reaches `hebb`'s anywhere in the sweep (4–11× smaller) | Matched on the wrong quantity. Superseded by C-22. The genuinely missing control was a **rank-1 random direction at matched σ₁** — T1.4 has since run it, and it did falsify C-22 (now `retired`; the surviving claim is C-55) |
| **C-24** | `A04_climate` changes **dynamical class** — a fixed point (lag-1 0.99999) becomes a period-2 orbit (lag-1 0.661) | `supported` | `exp001.jsonl`: nudged `cos_lag1_mean` 0.6606 against a frozen 0.9999992 for the same prompt | **The strongest result in the repository**, and the class change is what is strong: lag-1 is a property of the trajectory rather than of the readout, so no relabelling artifact can explain it. **The identification of the orbit as `Divine` is weaker than this row used to imply.** The range 0.659–0.696 it once cited is an n = 2 sample of frozen `Divine` prompts inside `exp001.jsonl`; against the committed 125-prompt census (`basins.jsonl`, 300 iterations) the 34 `Divine` prompts span **0.678–0.729** and the nudged 0.661 falls **below** it, with `instrument_validation.json` putting the canonical orbit at 0.684912. Any statement about *which* orbit it lands on must name its population and belongs to C-25, not here. n = 1 prompt |
| **C-25** | The `A04` transition is a **boundary move into a pre-existing attractor** — issue #25 ladder step 3 | `provisional` | `Divine` holds 34 of the 125 frozen baseline prompts, and a period-2 orbit exists there for the state to move into | Issue #25 calls step 3 "the measurable one, and the first real result". **Downgraded from `supported` 2026-08-05**: what is measured is that A04 becomes period-2 (C-24), and that a period-2 basin pre-exists. That the nudged orbit *is* that basin's orbit is not established, because the nudged lag-1 of 0.661 sits below the census range of 0.678–0.729 for the 34 frozen `Divine` prompts. The missing control is a direct state comparison against a settled `Divine` state, which no committed artifact contains |
| **C-26** | `comrade` is a **created attractor** — ladder step 4, a bifurcation | `retired` | **Refuted by T1.1, the test this row named as deciding** (`experiments/output_t1_1/T1_1_RESULTS.md`, `t1_1_trajectory.jsonl`, `meta.json`). Under `W0 + ΔW` seeded from the **original frozen `prolet` state**, the loop does **not** stay: it moves to `comrade`, settling at iteration 12 and holding through 120, with lag-1 ≥ 0.99990 at every step and the top1−top2 margin falling smoothly to 0.00025 at the crossing. One **displaced** attractor, not two coexisting — the frozen `prolet` state falls into the single `comrade` fixed point (lag-1 = 1.0). The created-attractor (step-4) reading required coexistence; it fails. What is measured is ladder **step 2**: a single fixed point the edit relocates continuously and the readout relabels across a ridge (consistent with C-27). eta=0 gate bit-identical to the frozen loop (max abs diff 0.0). Durable finding: **C-56** | **Verdict invariant** to the renorm shell. **Scope:** T1.1 refutes the specific coexistence claim (the original `prolet` state staying put); the independent hysteresis cross-check is **C-52 / T1.2**. `comrade` and `prolet` sit close at settling (margin 0.321, `prolet` rank 3) — within C-07 basin-resolution. n = 1 prompt, 1 site, 1 eta |
| **C-27** | The α-sweep shows a **threshold**, not a smooth bias (issue #32 §5) | `not-established` | The underlying logit gap is smooth and monotone through the crossing; only the argmax is discrete. Driven mainly by `prolet`'s logit *falling* (16.95→16.07), not `comrade`'s rising | An argmax always changes discretely, including under perfectly smooth motion |
| **C-28** | A genuine change of dynamical class occurs between α = 1.25 and 1.50 | `provisional` | lag-1 collapses 0.999998 → 0.734; state norm jumps 65.9 against a running increment of ~15 | Real, and the only qualitative transition in the sweep — but it lands in the **pre-existing** `Divine` basin (step 3, not 4), and sits at 1.5× the ΔW the episode produced |
| **C-55** | At `hebb`'s loop displacement, an **arbitrary** rank-1 direction usually moves the basin — but never to `hebb`'s destination, and only at 66×–171× `hebb`'s weight cost | `supported` | T1.4, 10 seeds per arm (`experiments/output_rank1_random/T1_4_RESULTS.md`, `rank1_random.jsonl`, `meta.json`): Arm B matched within ±2% on 6/10 seeds, **4/6 flip** (`Anarch` ×2, `bourgeois` ×2); destination `comrade` **0/10 seeds and 0 of all 74 probe evaluations**; drift cost ‖ΔW‖_F/‖W0‖_F **0.740–1.927** against `hebb`'s **0.0112**; Arm A (matched σ₁ = 1.8135) **0/10 flips**, displacements 4.0e+03–6.7e+05× smaller than `hebb`'s | n = 1 prompt (`A01_physics`), 1 site, 120-step episode. 4/10 seeds could not be matched — the flip-rate denominator is 6, not 10. **C-07 travels with the destinations**: 6 of the 8 flips land `Anarch`, whose gap to `prolet` is 2.874e-03, which is *below* the **pooled** mean within-basin spread of 3.319e-03. **State the population, because the comparison reverses if you do not**: 3.319e-03 is the mean over 2337 pairs pooled across all five basins (`BASELINE.md`), not `prolet`'s own, and `prolet`'s own within-basin spread is **2.773e-03**, which is *below* that gap. (An earlier version of this clause said "above", which was wrong: 2.773e-03 < 2.874e-03.) **So the coarse-instrument point does not apply to these destinations at all.** `Anarch`'s own spread is 1.178e-03, also below the gap, so both basins in the nearest pair resolve at their own scale. See C-07, which now carries the decomposition. Flip margins 0.020–0.684, no pre-registered ambiguity threshold (T0.5 pending). `bourgeois`, like `comrade`, is not among the 5 frozen baseline basins. Position uniformity holds at every recorded Arm B scale (min pairwise cos 0.99999999999993 at 0.39–2.20× ‖W0‖_F), which is what licenses the displacement match itself |
| **C-56** | The working-point edit `W0 + ΔW` (‖ΔW‖_F/‖W0‖_F = 0.0112) **displaces the single settled attractor** `prolet` → `comrade`; it does **not** create a `comrade` attractor beside a surviving `prolet` one | `supported` | T1.1 (`experiments/output_t1_1/T1_1_RESULTS.md`, `t1_1_trajectory.jsonl`, `meta.json`): seeded from the frozen `prolet` state under `W0 + ΔW`, the loop moves to `comrade` (settle iter 12, hold to 120, lag-1 = 1.0 at the settled fixed point; relL2 from the seed → 0.139). eta=0 gate bit-identical to the frozen loop; ΔW reproduced to ‖ΔW‖_F/‖W0‖_F = 0.011239339962675624 and σ₁ = 1.81352, 0/120 clip | **Displacement, not coexistence** — the dual of `BASIN_BIFURCATION.md` D1 (which slid the `comrade` state back to `prolet` under W0). Verdict invariant to renorm shell. Independently corroborated by T1.2/C-52 (clean retrace, no hysteresis). Settled `comrade`/`prolet` within C-07 basin-resolution (margin 0.321, `prolet` rank 3). n = 1 prompt (`A01_physics`), 1 site (`blocks.6.mlp`), 1 eta |

---

## D — The coupling question

| ID | Claim | Status | Evidence | Caveat |
|---|---|---|---|---|
| **C-30** | The severed-path control gives a null of **exactly zero** | `supported` | `exp001.jsonl`: all 5 severed cells `bit_identical: True`, `rel_fro_diff` exactly `0.0`, `diff_over_drift` exactly `0.0` | **Holds in `recomputed` mode only.** In `recorded` mode the severed cells are *not* bit-identical (`rel_fro_diff` 0.0081–0.0084) and the severed floor (0.287) *exceeds* the routed value (0.248) — sign reversed. `offline_control.py:883-891` already declares `recorded` uninterpretable as a feedback test, and documents why: recorded-y freezes the rule's own y-recursion in one arm only. Structural argument verified: `blocks.3.hook_resid_post` is a function of blocks 0–3 only, all 12 blocks still execute, so the plastic site fires with no causal path to the next iterate |
| **C-31** | **Feedback measurably steers the weight change.** The feedback-attributable component is 12% of total drift, against a null of exactly zero | `supported` | `EXP_001_RESULTS.md` §3: routed `diff_over_drift` 0.120 vs severed 0.000. Instrument round-off floor `1−cos` ≈ 1.5e-14, ten orders below the routed signal | The severed arm runs a shallower loop (0→3) at 2.6× the drift, so it is not a *statistically* matched control — the argument is structural (C-30). Independent n = 3 **Conditional on `y_source="recomputed"`** — see C-30. The two feedback-free offline arms (recorded-y vs recomputed-y) differ from *each other* by `diff_over_drift` 0.238–0.248, roughly twice the feedback-attributable 0.112–0.120; `y_source` is deliberately not one of the 17 axes, and the severed arm is what shows its contribution is exactly zero when the path is cut. The 0.120 headline is normalised by `‖ΔW_offline‖` (`offline_control.py:1017`, `diff / max(drift)`, documented at `:990`); against `‖ΔW_closed‖` it is 0.124 |
| **C-32** | The steering is predominantly a change of **direction**: a **6.81°** rotation with a **2.8%** shortening | `provisional` | cos 0.99294 → 6.81° (A01; 6.32° A02, 6.37° A04); `‖ΔW_closed‖/‖ΔW_offline‖` = 0.97225 | **Do not quote a perpendicular:parallel ratio.** It is the deterministic function `r·sinθ/\|r·cosθ−1\|` of those two numbers, takes 3.33 or 5.73 depending on reference, is ill-conditioned (`d ln ratio/d ln r` = −47, pole 2.1% away), and ranges 5.03–7.67 across prompts. At this cosine perpendicular dominance is automatic for any norm ratio in [0.900, 1.144] — at *equal* norms it would be 16.8, so the measured value is **less** direction-dominated than equal norms would give |
| **C-33** | At this step size and horizon, feedback does **not** change the behavioural outcome | `supported` | Both arms flip to the same basin on all 3 prompts | The honest limit on C-31, and it must travel with it |
| **C-34** | "The two arms agree, so feedback did nothing" | `retired` | Contradicted by C-30 + C-31. Issue #26's rubric ("near 1 → nothing but scale") was written before the severed control existed to calibrate what "near 1" means | Still the reading in `HANDOVER.md` §3.3 and the issue #32 comment |
| **C-58** | The feedback-attributable share of the weight change **grows monotonically with coupling** along all three axes (step size, firing cadence, episode length), superlinearly in episode length; the behavioural outcome **never differs between the connected and matched-offline arms at any ceiling-silent setting in the T2.1 grid (up through 2x eta, 240 steps)** | `supported` | T2.1 (`experiments/output_t2_1/t2_1_coupling.jsonl`, `T2_1_RESULTS.md`, `meta.json`): 9 routed cells + 1 severed, 17/17 axes matched on every cell, severed recomputed floor exactly 0.0 and bit-identical, base cell reproduces EXP-001's 0.1203852 to 7 significant figures. Shares (recomputed): eta ladder 0.0262 → 0.0549 → 0.1204 → 0.2722 (0.25x → 2x, all 0 clip); cadence 2/4: 0.0543/0.0254; steps 60/240: 0.0266/0.3422. Halving eta and halving cadence agree to within 1.2% of each other; episode length compounds faster than proportional (60->120 multiplies the share by 4.52, 120->240 by 2.84; both doublings exceed 2x). Outcomes: `prolet`/`prolet` below the flip, `comrade`/`comrade` at the working point, **`Divine`/`Divine` (genuine period-2, lag-1 0.672-0.751, lag-2 = 1.0, margins 2.029-2.379) at 2x eta and at 240 steps, ceiling silent, both arms** | Conditional on `y_source="recomputed"` (C-30, C-31 caveats travel). The only arms-disagreement observed is the 4x-eta cell where both arms saturated the `max_delta_frac` 0.05 ceiling (closed: `Divine`; offline: a token outside the 5 baseline families): diagnostic under standing rule 2, never quotable. n = 1 prompt (`A01_physics`), 1 site (`blocks.6.mlp`), 1 seed. The `Divine` crossings extend C-28's class change to live episodes at the working step size (240 steps) and show it does **not** require feedback. **Boundary attached by T2.1b/C-59**: the agreement clause holds up through 2x eta and 240 steps; the first ceiling-silent disagreement sits at 2.5x eta |
| **C-59** | At eta = **2.5x** the working step size, **feedback changes the behavioural outcome**, ceiling silent: the connected loop settles into the period-2 `Divine` cycle (lag-1 0.685, lag-2 1.000, margin 2.36) while the feedback-severed arm ends on a **near-stationary trajectory** (late-window lag-1 0.998; not classified as a fixed point, no convergence test was run) reading out `【`, a token outside the five baseline basin families | `supported` | T2.1b (`experiments/output_t2_1/t2_1b_gap.jsonl`, `T2_1B_RESULTS.md`; pre-registered in `PREREGISTRATION_T2_1B.md` before the run): closed drift 0.0403 and offline drift 0.0465 against the 0.05 cap, 0 clip on all arms, 17/17 axes matched, both margins above the pre-registered 0.05-logit threshold by a factor of 40+. Onset localized to (2x, 2.5x] at 0.5x-grid resolution; the 3x and 3.5x cells repeat the same split but saturate the ceiling and are diagnostic only | **The first admissible feedback-changes-outcome observation in the project.** Conditional on `y_source="recomputed"` (C-30/C-31 caveats travel). n = 1 prompt (`A01_physics`), 1 site, 1 seed, `hebb` only. Share does not predict divergence: the agreeing 240-step cell carries share 0.3422 vs this cell's 0.3496; the axis matters. What `【` "is" is not claimed beyond the observed terminal readout; the severed arm's dynamical class is **unclassified** (late-window lag-1 0.998 does not establish convergence), so the established contrast is the basin-label disagreement plus the connected arm's verified period-2 signature, not a class-vs-class statement. A convergence-classified rerun is the named follow-up |
| **C-60** | Plasticity at **all twelve MLP layers at once** moves the driven prompt's settled word, and the with-feedback and without-feedback runs settle on **different** words | `supported` | EXP-002 (`experiments/output_exp002/exp002_uncapped.jsonl`, `EXP_002_RESULTS.md`; pre-registered before the run). `hebb` at 1.31% aggregate drift: `prolet` (margin 0.230) → **`Rousse`** (0.582) with feedback, **`comrade`** (0.307) without; all three exact fixed points (lag-1 = lag-2 = 1.000000). `anti_hebb` at 5.38%: → `arcane` with feedback, `Shiv` without. Gates: eta=0 bit-identical (max abs diff 0.0), `revert()` bit-exact on all twelve matrices, no non-finite, collapse step reproduces the baseline `prolet` | **Ceiling lifted** (`max_delta_frac` 1e9, operator decision, Amendment 2): every prior repository result was taken under a 5% cap and this was not, so it is a different regime and may not be quoted as continuous with them. Per-layer step sizes anchored to a common 1.12% target (Amendment 1) because one shared step size spans 136x across layers for `hebb` and 56x for `anti_hebb` (`exp002_uncapped.jsonl` `calibration.probe_per_site[].delta_frac`; the >200x once quoted here pooled the two modes, which is not a span across layers); `hebb` anchored well (1.03–1.56%), `anti_hebb` badly (0.92–11.18%, 12x). The two arms are **not** matched on drift. Attribution of the with/without difference is limited by C-63. n = 1 driven prompt, 1 seed, MLP down-projections only |
| **C-61** | **Something carries across the prompt boundary**: after distributed plasticity, **every** fresh prompt the drifting never saw changes its settled word | `supported` | EXP-002, 31 fresh prompts stratified over all five baseline basins, driven prompt excluded, each run frozen under each weight set. Reference gate: **31/31** reproduce the committed 125-prompt census exactly before any drift. After drift: `hebb` with feedback 31/31 changed, without feedback 30/31; `anti_hebb` 31/31 in both conditions | Issue #29's phrasing is binding: this is **a measured difference in behaviour on fresh prompts**, and the word *learning* is used nowhere. What carried is destructive (C-62), so "carries across" must never be quoted without it. 31 prompts, not the full 125 census |
| **C-62** | What carries across is **collapse, not steering**: the basin census is destroyed in every arm tested, by one of two mechanisms | `supported` | EXP-002 reprompt tables. Untouched: 5 distinct settled words (prolet 13, Divine 8, till 5, Anarch 4, solidarity 1), 31/31 at rest. `hebb` with feedback: 3 words, **27/31 on `Rousse`**, 31/31 at rest; without feedback: 2 words, **30/31 on `comrade`**. `anti_hebb` without feedback: **1 word**, all 31 on `Shiv`. `anti_hebb` with feedback: 19 words but **only 4/31 at rest** — trajectories still in motion at iteration 120, which is non-convergence and **not** surviving structure. Return test, **phase-aware over the last two iterates with five independent directions**: `hebb` **5/5** returns (exact fixed points, 1–30 iterations); `anti_hebb` **0/5**, flooring at ~1e-4 regardless of perturbation size (1e-7 to 1e-1), with its best match the *last* iterate at every magnitude, so a missed period-2 orbit is excluded — issue #26's fourth outcome, wandering without settling or diverging | **The pre-registered criterion was too weak and is corrected, not invoked.** It asked only that "more than one basin remains occupied", which a literal reading would score as a pass; 27 of 31 prompts on one word is collapse, as issue #26 states directly. The criterion should have compared the distribution against the baseline. `hebb` leaves a genuine attractor that swallows nearly everything; `anti_hebb` with feedback leaves the system unable to settle at all. Both destroy the census. **Return-test limit:** phase-aware over the last two iterates, so it separates a fixed point and a period-2 cycle from neither; a longer-period orbit would still score as a failure. The first implementation was single-phase and reused one perturbation direction across all five magnitudes; both were corrected and the test re-run, and neither verdict changed |
| **C-63** | **The severed-path control's exact-zero floor is a single-site guarantee and does not extend to plasticity at more than one layer** | `supported` | EXP-002 severed gate, eight plastic layers (`blocks.4.mlp`…`blocks.11.mlp`) read at `blocks.3.hook_resid_post`: `blocks.4.mlp`, the **lowest** plastic layer, gives closed−offline difference **exactly 0.000000, bit-identical True**; every layer above is non-zero, with difference/drift broadly increasing with height though not monotonically (0.033, 0.143, 0.152, 0.170, 0.186, 0.159, 0.245). The severed control cuts a plastic layer's path into the **next loop iterate**; for the lowest layer that is the only path. Higher layers keep a second path it cannot cut: **within one forward pass, a lower plastic layer's drift changes the activations arriving at a higher one**, which does not involve the loop | **Amends standing rule 3**, which states the severed floor is exactly zero and is the baseline for any claim that feedback did something. True at one site; false beyond it. Consequence: every closed-versus-offline number in EXP-002 measures loop feedback **and/or** within-pass layer interaction with **no zero baseline**, so the measured shares (`hebb` 0.4373, `anti_hebb` 0.2576) are **not comparable** to C-31's 0.12 or C-58's ladder and must never be placed in a series with them. The pre-registration anticipated the two effects being inseparable; it did **not** anticipate the floor ceasing to be zero, and that is recorded as a correction |
| **C-35** | Does the feedback-attributable component **grow** with coupling strength, and is there a regime where it changes the outcome? | `open` | **Answered by T2.1** (`experiments/output_t2_1/`): the share **grows**, monotonically along all three coupling axes, and **no ceiling-silent setting inside the T2.1 grid changes the outcome** — a clause that is bounded, not general: **C-59** found the first ceiling-silent setting that does change it, at 2.5x eta, just outside this grid. Ladder (recomputed, severed floor exactly 0.0): eta 0.25x/0.5x/1x/2x gives 0.0262/0.0549/0.1204/0.2722; cadence 2/4 gives 0.0543/0.0254; steps 60/120/240 gives 0.0266/0.1204/0.3422, superlinear in episode length. Arms agree on the settled basin in every ceiling-silent cell, including two changes of dynamical class into `Divine` (2x eta and 240 steps, both arms, 0 clip). The one disagreement sits in the 4x-eta cell where **both arms saturated the 0.05 ceiling**: diagnostic only, never quotable. Durable claim: **C-58** | The highest-value experiment in the project; answered as "grows, but never changes the outcome inside the safe range tested". The row stays `open`-styled per register convention for answered questions (see C-50, C-51, C-52) |
| **C-36** | ΔW's dominant direction carries no logit-lens-legible preference for the basin tokens | `not-established` | The null 5–95% bands span 91 percentile points (`prolet` [7.7, 98.4]) — the test cannot reject anything | Reported as an absence in `EXP_001_RESULTS.md` §4. It is an absence of power, not an absence of effect |

---

## E — Scope, novelty and infrastructure

| ID | Claim | Status | Evidence | Caveat |
|---|---|---|---|---|
| **C-40** | Every number in the repository is **one site** (`blocks.6.mlp`), one model, cadence 1 | `retired` | **Contradicted by the repository's own committed artifacts.** All twelve MLP down-projections `blocks.0.mlp`…`blocks.11.mlp` carry plasticity in `output_exp002/exp002.jsonl`, `output_exp002/exp002_uncapped.jsonl` and `output_exp003/stage1.jsonl`; cadence 2 and 4 run in `output_t2_1/t2_1_coupling.jsonl`, and cadence 4 and 12 alongside 1 (field `k`, `ladder: [1, 4, 12]`) in `output_exp003/stage2.jsonl` | Retired for staleness, not for error: it was true when written. The corrected facts were **already in this register** — C-60 records the twelve layers, C-58 the cadence ladder — while this row still read "one site, cadence 1", so it was not updated when they landed. The "one model" clause and the narrow-scope point both survive with different numbers at **C-64**, which is what `experiments/output_exp003/PREREGISTRATION.md` should cite in place of this row |
| **C-41** | Independent n is **3 prompts**, not the 5 cells the tables imply | `supported` | `hebb` has no stochastic term, so seeds 0/1/2 are one run three times — spread exactly 0.000e+00 | `EXP_001_RESULTS.md` §3's "n=5" medians are forced onto A01's value, which is the extreme of the range in 3 of 4 rows |
| **C-42** | No work combines a hand-written local rule, a pretrained frozen model, a closed activation loop, and no objective | `provisional` | `PRIOR_ART.md` — and the file is explicit that this is a statement about a search, not about the literature | **11 absence claims have no preserved artifact.** The search was term-driven, not concept-driven, and never queried test-time adaptation (TENT), model editing (ROME/MEMIT), fast weights (Ba et al.), modern Hopfield (Ramsauer), predictive coding/DEQ, or Hebbian-at-scale (SoftHebb). Per C-11, "no loss" is the only load-bearing axis and it is softer than stated |
| **C-43** | ROME already makes rank-1 edits to a mid-stack GPT-2 MLP down-projection | `open` | Not yet in `PRIOR_ART.md` | The novel element is **not** "a rank-1 edit there changes behaviour". It is that the edit is derived with **no target**, from the model's own activation statistics, and read out on the **iterated map's attractor structure**. State it that way or a reviewer will say ROME first |
| **C-44** | Chaudhary 2025 (arXiv:2510.21908) §4.8: Hebbian plasticity stays stable at 8 layers and saturates (recall 0.729 ± 0.015); gradient-plastic diverges after ~3000 steps, deepest layers drifting most | `supported` | Quote recorded in `PRIOR_ART.md` and issue #27, **independently re-fetched during the alignment review** and matching the source word for word, including the §4.8 title *Task-Dependent Behaviour and Depth Stress Test* | **Agent-verified across three independent retrievals; still no human has opened the PDF.** That is the remaining gap, and it is small: `PRIOR_ART.md`'s original fetch, a direct re-fetch during the alignment review, and a third-party web query all return the passage verbatim. **A negative retrieval is weak evidence** — the third-party tool's first query reported that no such section exists, and its second, more specific query returned the section's findings word for word. Absence of a hit is not absence of the text. **Two limits that do bite:** the paper reports no 4-layer result at all (standard models are 2 layers, stress test 8 — "a practical regime around 4 layers" is interpolated), and **nothing in this literature tests 12 layers with any rule**, so it cannot set an expectation either way for GPT-2 small. Use as corroboration of the family split, never as an expectation for this substrate |
| **C-45** | The offline arm's `recomputed`-y path was **not** trustworthy at head sites (pre-fix) | `retired` | Pre-fix `offline_control.py:791` reconstructed `y` from one head's contribution (`x @ W_head`) rather than the shared full output. **Reproduced on real GPT-2 small (T1.5, `transformer-lens 3.6.0`):** recomputed-y relative error vs full output **≈ 1.00** (per-sample 0.9988–1.0021), head-site `recomputed` severed floor **4.9975e-02** (saturates near the 0.05 `max_delta_frac` ceiling) against the whole-matrix 0.000e+00. Stand-in figures (0.838 / 3.87e-04) did **not** transfer; the real severed floor is two orders larger. **Fixed in T1.5** by additive reconstruction of the shared full output (`record.y + x @ delta`, gated on `site.shared_post_activity`): the head-site `recomputed` severed floor drops from **O(1e-2) to float32 noise 2.960e-08** (`bit_identical False`) — **NOT exactly 0.0**. | **Invisible to all 17 matched axes** (`y_source` is not an axis). The severed-path control **does** flag it. No published result affected: **no committed experiment applies plasticity at a head site** (C-64) — a narrower and still-true statement than the "every committed artifact is `blocks.6.mlp`" this caveat used to give, which C-40's retirement withdrew. **The exact-zero severed floor is a whole-matrix property only** — at a head site `y` is a fused twelve-head einsum reconstructed additively, so its null is a measured float-noise bound (~1e-7 class), not 0.0. Post-fix correctness → C-57. |
| **C-57** | At a per-head site the offline arm's `recomputed`-y path reconstructs the **shared full** projection output, so the severed no-feedback floor is float32 noise, not O(1) | `supported` | `offline_control.py` `replay_offline` branches on `site.shared_post_activity` and rebuilds the drifted full output additively as `record.y + x @ delta` (other heads and `b_O` frozen in the recorded `y`). On real GPT-2 small at `blocks.11.attn.head.7`: unit reconstruction rel error to the full output **0.0** at eta=0 (`test_recomputed_y_at_a_head_site_is_the_shared_full_output`); severed `recomputed` `rel_fro_diff` **2.960e-08**, `bit_identical False` (`test_a_head_site_the_loop_does_not_route_through`), against the pre-fix 4.9975e-02. | The severed floor is **float32 noise (~1e-7 class), NOT exactly 0.0** — a fused twelve-head einsum cannot be reconstructed bit-for-bit from one head's additive remainder, so the null here is a measured bound (test uses `1e-6`), unlike the whole-matrix site's exact 0.0. The `recorded`-y path is unchanged (head-site severed floor 8.525e-05). Whole-matrix `blocks.6.mlp` path untouched and still bit-exact (0.0). |
| **C-46** | The library records a clipping **rate** | `retired` | `clipped` is a latching boolean that only clears on `revert()`. `step_size_map.py` synthesises a rate by writing to semi-private state; `exp001_hebb.py` records the boolean and documents the limit | **Propagation closed 2026-08-01.** `ORIENTATION.md` over-claimed in two places, the ceiling paragraph and the measured-quantities list; both now state that `report()` gives a latching boolean and that the rate is the experiment script's own work. `README.md` was already accurate |
| **C-47** | The 125 settled states are saved so within-basin spread can be re-measured with no further model time | `retired` | `.gitignore:27` excludes `experiments/**/states/`; the directory does not exist in the repo | The summary statistics in `BASELINE.md` survive (C-04 – C-07); the raw states do not. Any extension needs the ~6 CPU-hour baseline re-run |
| **C-64** | Every committed experiment is **GPT-2 small**, and every **plastic** site in one is an **MLP output projection**: 12 of 12 MLP down-projections have carried plasticity, **0 of 12** attention output projections and **0 of 144** head stripes have. Cadence **1, 2, 4 and 12** have been run | `supported` | Enumerated over every committed `experiments/output_*/*.jsonl`. Sites: `blocks.6.mlp` alone in `exp001.jsonl`, `step_size_map.jsonl`, `t2_1_coupling.jsonl`, `t2_1b_gap.jsonl`; all twelve `blocks.{0..11}.mlp` in `exp002_uncapped.jsonl` and `stage1.jsonl`, and at `exp003_stage2.py:74`; in the capped `exp002.jsonl` the twelve appear only in `calibration` rows, which that artifact itself annotates as declared non-evidence, so the plasticity episode is the uncapped run's. Cadence: 1 (EXP-001), 1, 2 and 4 (`t2_1_coupling.jsonl`), 1, 4 and 12 (`stage2.jsonl` field `k`, `ladder: [1, 4, 12]`). Model: `gpt2-small` in all nine committed metas that record the field; EXP-003's artifacts do not record it and its runners hardcode `from_pretrained("gpt2")` (`exp003_stage0.py:271`, `exp003_stage1.py:112`) | Replaces **C-40**, and carries the scope point C-40 was cited for: **this is still the project's weakest dimension** — 12 of 168 candidate sites, one model, one site family. The 144 head stripes have never carried plasticity in a committed experiment; they are exercised by the suite only (C-45, C-57). **The multi-site runs may not be placed in one series with the single-site ones**: they lift the ceiling (C-60) and lose the exact-zero severed floor (C-63), so wider coverage may not be assembled by adding them up. EXP-003 Stage 0 *measures* activity at all 144 head sites and applies plasticity at none. Cadence > 1 is tested at one site only (`t2_1_coupling.jsonl`) except for `stage2.jsonl`, whose own registered drift guard fired |

---

## F — EXP-003: the borrowed activity measurement, and what it refuted

**Why these rows exist.** EXP-003's three completed stages were written up with each results file
stating that nothing in it entered this register. That left three pre-registered measurements —
one of them a refutation of a mechanism this project had proposed for its own collapse —
committed to the repository but unquotable, which contradicts this file's own first rule that a
claim enters the register before it enters any prose document. The rows below fix that. They
cover the **measurements only**. The cultured-network argument that once accompanied them was
removed at the operator's instruction and is not reinstated here; `MEA_SOURCES.md` holds the
source material for the borrowed statistics and no reading of it.

**Regime, for all three rows: the drift ceiling is lifted** (C-60), so nothing here is
continuous with any result taken under the 5% cap.

| ID | Claim | Status | Evidence | Caveat |
|---|---|---|---|---|
| **C-65** | The depth-weighted activity centroid **separates the five end states and does not separate fixed points from two-step cycles** | `supported` | Stage 0 (`experiments/output_exp003/stage0_analysis.json`, `stage0.jsonl`, thresholds registered in `PREREGISTRATION.md` before the run), 125 committed baseline inputs, frozen model, no plasticity, 120 iterations, centroid averaged over the last 25. Between-group over within-group spread: end states **12.2035** against a registered pass threshold of 1.5; dynamical class **1.0036**, which **fails** the same threshold. Negative control: 10 of 10 random halves of the largest group score below 1.2. Label controls: permuting block labels gives 0.3701 against the true 12.2035, permuting head labels moves the statistic by **1.78e-15** against a registered 1e-9 tolerance | **Gate 2's failure is the load-bearing part of this row.** A statistic that cannot tell a fixed point from a two-step cycle is blind to exactly the distinction carrying **C-24**, the strongest row in this register, so it may not be used to argue anything about dynamical class. The 12.2035 is a separation of *labels the register already treats as close* — C-07's resolution limit still applies to the groups being separated. The head-label invariance is a structural property of a depth-weighted centroid rather than a finding. Two post-registration variants are recorded in the artifact and labelled as such; the "settle profile" variant scores **1.4303** on end states and therefore **fails** the 1.5 threshold, so the registered scalar is the only variant that passes and the others must not be quoted as corroboration |
| **C-66** | **Spectral concentration at the plastic sites is not the mechanism of the distributed collapse.** Reproducing EXP-002's reinforcing episode moves mean effective rank by **0.044%**, against a pre-registered refutation threshold of 2% | `supported` | Stage 1 (`experiments/output_exp003/stage1.jsonl`, `STAGE1_RESULTS.md`; thresholds registered before the run: `supported` at a fall of 10% or more, `refuted` below 2%). Mean effective rank 639.4554 frozen against 639.1740 drifted, a fractional fall of 0.00044; mean top singular share **rises** 0.399%, 0.007766 to 0.007797; largest single-site fall 0.244% at `blocks.2.mlp`. Reproduction gate exact: aggregate drift 0.013111766434820447 against EXP-002's committed 0.013111766434820447, relative miss **0.0**, 0 clip, 0 non-finite. The 10% threshold was set against a stated reference, the largest effective-rank movement anywhere in the committed step-size map being a rise of about 0.6% | **This refutes one named mechanism; it does not explain the collapse, and it does not weaken C-62.** Effective rank and top singular share are two specific quantities and neither measures how well a matrix separates the states this loop visits, which is the thing collapse is about. Two measurements registered for this stage were **never implemented** and are recorded as not run rather than as absent findings: the depth-weighted centre of the weight change across the twelve sites, and the smallest weight change at which each statistic separates the drifted system from the frozen one. Ceiling lifted. 1 driven input, 1 seed, 1 rule, 12 MLP sites |
| **C-67** | Census agreement is **0 of 31 at every cadence tested** — every 1st, 4th and 12th iteration — at drifts of 1.31%, 2.99% and 7.91%, with nothing clipping | `supported` | Stage 2 (`experiments/output_exp003/stage2.jsonl`, `STAGE2_RESULTS.md`, amended by Amendment 1 before the run). Reference gate: the 31 fresh inputs reproduce the committed baseline **31 of 31** before any weight moves. All three cells settle 31 of 31 and agree with the census 0 of 31, on `Rousse` 25 / `anarchism` 5 / `comrade` 1, then `observer` 31, then `.` 31. The cadence-1 cell reproduces EXP-002's drift to sixteen significant figures | **The cadence comparison this stage was built to make is NOT established, and the row does not assert it.** The runner scaled step size by cadence assuming drift is linear in step size; it is not, and the registered guard caught it: drift spread across the ladder is **6.03×** against a required factor of 2, so `drift_matched` is false, the comparison is recorded as qualitative only and the falsifier was **not** invoked. Cadence is therefore confounded with drift and nothing here separates a slow-timescale effect from a larger-drift effect. Two further limits: the non-finite flag was not recorded in this run, so no claim is made about it, and this run's 31 fresh inputs differ from EXP-002's by two, because the two runners implement the same stratified selection rule separately and were never checked against each other. Ceiling lifted. 1 driven input, 1 seed, 1 rule |

---

## G — A discrepancy between two committed artifacts

| ID | Claim | Status | Evidence | Caveat |
|---|---|---|---|---|
| **C-68** | At **α = 0.50** the edited map `W0 + 0.5·ΔW` reaches **two different settled words from two different initial states**: `prolet` from the prompt's iteration-0 residual tensor, and `comrade` from a settled `prolet` state | `provisional` | Two committed artifacts, same site, same rule, same eta, same prompt, same 120 steps, same ΔW (`delta_frac` 0.011239339962675624 and σ₁ 1.8135175 in both metas), same TransformerLens 3.6.0. `output_basin_bifurcation/basin_bifurcation.jsonl`, D2 at α = 0.5: settled `prolet`, margin 0.1144, lag-1 0.9999996, 15 of 15 tail iterations `prolet`, `comrade` at rank 2. `output_t1_2/t1_2_hysteresis.jsonl` at α = 0.5: settled `comrade`, `is_settled` true, in **all four** sweep arms including `robust_up`, whose seed word is `prolet`. The seeding differs and is the candidate explanation: D2 starts from the prompt's iteration-0 tensor, T1.2 is a continuation sweep seeded from the previous α's settled state (`meta.json` `seed_rule`) | **Not a matched pair, which is why this is `provisional` and not `supported`.** The two runs were designed for different purposes and differ in initial condition, so the comparison is opportunistic. What it does establish is that **the inference C-52 draws — one continuously-moving fixed point rather than coexistence — is stronger than the evidence at α = 0.50**, because at that α two initial states reach two different settled words under identical weights. It does **not** touch **C-56**: T1.1 tested α = 1.00 directly, seeding the frozen `prolet` state and watching it move to `comrade`, and that result stands. Note the D2 margin at this α is 0.1144, and the `prolet`/`comrade` logit gap 0.114, so this sits inside C-07's resolution limit and could be a readout artifact rather than genuine bistability. **The test that would settle it** is a basin-of-attraction probe at α = 0.50: seed the same map from several initial states, including both settled words and the iteration-0 tensor, with a convergence criterion fixed in advance. Until that runs, no document may describe the α sweep as showing a single attractor at every α |

---

## Open questions, tracked

| ID | Question | Where |
|---|---|---|
| **C-35** | Does the feedback-attributable share grow with coupling strength, and does feedback ever change the outcome? | **Answered by T2.1 + T2.1b: the share grows, monotone on all three axes; the outcome is feedback-independent through the T2.1 grid (2x eta, 240 steps) and feedback-dependent at 2.5x eta, ceiling silent.** Durable claims C-58, C-59. Evidence: `experiments/output_t2_1/` |
| **C-50** | Does a rank-1 random edit at matched σ₁ **and matched loop-state displacement** flip the basin? | **Answered by T1.4: yes — 4/6 at matched displacement, never to `comrade`.** C-22 retired; the surviving claim is C-55 |
| **C-51** | Under `W0 + ΔW`, does the original `prolet` state stay put? | **Answered by T1.1: no — it moves to `comrade`, settling at iteration 12 and holding through 120 (lag-1 ≥ 0.99990 the whole way, a smooth ridge-crossing).** One displaced attractor, not two coexisting; C-26 refuted → `retired`; durable claim C-56. Evidence: `experiments/output_t1_1/` |
| **C-52** | Does the α-sweep show hysteresis? | **Answered by T1.2: no.** Continuation up-then-down α-sweep (step 0.10, 0→1.5→0) **retraces exactly** — both directions give the identical basin map (`prolet` at α ≤ 0.4, `comrade` at α ≥ 0.5); alpha_up 0.5, alpha_down 0.4, gap = one grid step (a single threshold near α ≈ 0.45, no resolved loop). Same verdict under the fixed-W0 shell (`robustness_agrees`). All settled states are fixed points (lag-1 = 1.0). **Corroborates C-26 `retired` and C-56.** Caveats: a sub-0.10 loop inside (0.4, 0.5) is not excluded at this grid; n = 1 prompt, 1 site, 1 eta. **And the "one continuously-moving fixed point, not coexistence" gloss this row used to carry is withdrawn as too strong** — see **C-68**, which records that D2 reaches `prolet` at α = 0.50 from the prompt's iteration-0 tensor where this sweep reaches `comrade` from a settled state. The absence of hysteresis along a continuation path is what was measured; it does not by itself exclude coexistence. Evidence: `experiments/output_t1_2/` |
| **C-53** | Does the persistence claim survive a fresh prompt? (EXP-002, issue #24) | **Answered by EXP-002, which ran as T2.2 and is merged** (`experiments/output_exp002/`, pre-registered in `PREREGISTRATION.md` before the run; issue #24 closed). **Yes, something crosses the prompt boundary — and what crosses is destructive.** All 31 fresh prompts reproduce the committed census before any drift, and after it the settled word changes on **31 of 31 in three of the four arms and 30 of 31 in the fourth** (`hebb` with feedback 31/31, without feedback 30/31; `anti_hebb` 31/31 in both conditions), while the five-basin census is destroyed in every arm. Durable claims **C-61** (something carries) and **C-62** (what carries is collapse, not steering) — neither may be quoted without the other. Attribution of the with/without difference is bounded by **C-63**. 31 prompts, not the full 125: that remainder is **C-54** |
| **C-54** | What does the 125-prompt library do at the working point? | T2.3 — largest credibility gain available |

---

## Standing prohibitions

Carried from `HANDOVER.md` §7, which learned each of these the hard way. They are not
negotiable and they are why the rows above are worth anything.

- **C0 is the gate.** At eta = 0 the hooks must not perturb the trajectory by a single bit.
- **A control that cannot fail is worse than no control.** Every control is tested in both
  directions.
- **Never reimplement the ATR loop.** Import it via `atr_bridge`.
- **Report the clipping rate with every result** — and note C-46, that the library does not
  give you one for free.
- **Never quote a cell where the ceiling fired.** Two citations were flagged here as live.
  Both were checked on 2026-08-01 and the entry now reads: `STEP_SIZE_MAP.md` §5's
  effective-rank evidence **was** in breach, bounding the effect with cells at 99.2% and
  60.8% clip, and has been rewritten onto the 23 ceiling-silent cells (effective rank never
  below 642.4 against a frozen 642.6, rising to 646.7 under `anti_hebb` at 0.0% clip); the
  two withdrawn figures are named in place and kept in the full table as diagnostics.
  `HANDOVER.md`'s `anarchism` citation is **not** in breach: it appears in the step-size
  summary table with "only at 100% clip" stated inline, in a column asking whether the rule
  moves the basin inside the ceiling-silent band, answered "no". Quoting a ceiling-fired
  cell as a non-finding with its clip rate attached is what this prohibition asks for.
- **Never use an even-only snapshot schedule.** Log lag-1 *and* lag-2.
- **Use tolerance, not `torch.equal`,** except where asserting bit-identity on purpose.
- **Don't tune to match the parent's basin percentages.** The bridge is bit-exact and
  CI-enforced; a mismatch means something else moved.
- **Say what you did not rule out.** Issue #27 is the list of failure modes that look like
  findings.
- **This is not learning.** The defensible phrase is that the weights carry a trace of the
  episode — and per C-11, "no externally specified objective" rather than "no objective".

---

*Every row that asserts something cites an artifact already committed — with the two exceptions
declared above: `open` rows cite no evidence by definition, and a row whose claim **is** an
absence cites the search that failed rather than an artifact. Where a row and the code disagree,
the code is right — open a PR and change the row.*
